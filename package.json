{
  "name": "analyst-in-action",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy && npx pagefind --site _site",
    "start": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "pagefind": "^1.4.0"
  }
}